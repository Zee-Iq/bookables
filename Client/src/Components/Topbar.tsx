import { AppBar, Toolbar, Typography } from "@mui/material";





const Topbar = () => {
  return (
    <AppBar position="sticky">
      <Toolbar>
        <Typography>Bookables</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Topbar;
